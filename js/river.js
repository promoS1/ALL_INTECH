"use strict";

// RIVER

var contenuRiver = fs.readFileSync("./json/river.json", "UTF");
var river = JSON.parse(contenuRiver);

var distributionRiver = function(){

	var i;

	for(i = 0;i < river.length; i++){
		
		
	}
}
